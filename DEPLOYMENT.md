# دليل النشر والإطلاق لمشروع VibraCam

هذا الدليل يشرح كيفية نشر منصة VibraCam على أي خادم (Server) باستخدام تقنيات قياسية مثل Docker و MySQL، بعيدًا عن بيئة التطوير الأصلية.

## 1. المتطلبات الأساسية للنشر المستقل

المشروع مستقل عن منصات الاستضافة الخاصة، ويستخدم المصادقة المحلية وJWT، وقاعدة MySQL، وتخزين S3 متوافق، وخدمات API مباشرة اختيارية.

### أ. قاعدة البيانات (MySQL)
المشروع جاهز للعمل مع أي قاعدة بيانات MySQL 8.0+.
- في بيئة `docker-compose` المرفقة، سيتم إنشاء قاعدة البيانات محليًا.
- في بيئة الإنتاج الحقيقية، يُفضل استخدام قاعدة بيانات مُدارة (مثل AWS RDS أو PlanetScale أو TiDB).

### ب. تخزين الملفات (Storage)
يدعم المشروع الآن وضعين للتخزين:
1. **التخزين المحلي (Local Storage)**: يتم حفظ الصور والفيديوهات في مجلد محلي على الخادم. هذا الوضع مفعل افتراضيًا في `docker-compose.yml` عبر المسار `/app/data/storage`.
2. **التخزين السحابي (S3)**: يمكنك استخدام أي خدمة متوافقة مع S3 (مثل AWS S3, Cloudflare R2, DigitalOcean Spaces) بتوفير المفاتيح المناسبة.

### ج. البريد الإلكتروني (Email)
يعتمد المشروع على خدمة **Resend** لإرسال رسائل تأكيد الحساب واستعادة كلمة المرور.
- يجب إنشاء حساب في Resend.
- الحصول على `RESEND_API_KEY`.
- التحقق من نطاقك وتعيين `RESEND_FROM_EMAIL` (مثل `noreply@yourdomain.com`).

### د. خادم TURN (للمكالمات العشوائية والفيديو)
لضمان نجاح مكالمات الفيديو (WebRTC) بين جميع المستخدمين متجاوزين الجدران النارية (Firewalls) والـ NAT، يجب توفير خادم TURN حقيقي.
- يمكنك إعداد خادم Coturn الخاص بك، أو استخدام خدمة مُدارة مثل Twilio أو Metered.
- المتغيرات المطلوبة: `TURN_SERVER_URL`، `TURN_USERNAME`، `TURN_PASSWORD`.

---

## 2. خطوات النشر باستخدام Docker

أسهل طريقة لنشر المشروع هي استخدام Docker و Docker Compose المرفقين.

### الخطوة 1: إعداد متغيرات البيئة
قم بنسخ ملف المتغيرات الافتراضي:
```bash
cp .env.example .env.production
```

افتح ملف `.env.production` وقم بتعديل القيم الحساسة:
- غيّر `JWT_SECRET` إلى نص عشوائي طويل (32 حرفًا على الأقل).
- غيّر كلمات مرور قاعدة البيانات `MYSQL_PASSWORD` و `MYSQL_ROOT_PASSWORD`.
- اضبط `PUBLIC_URL` و `CLIENT_URL` إلى النطاق الفعلي لموقعك (مثل `https://vibracam.com`).
- أضف مفاتيح Resend و TURN.

### الخطوة 2: تشغيل المشروع
قم ببناء وتشغيل الحاويات:
```bash
docker-compose --env-file .env.production up -d --build
```

### الخطوة 3: تهيئة قاعدة البيانات (Migrations)
بعد تشغيل الحاويات، يجب تطبيق مخطط قاعدة البيانات:
```bash
docker-compose exec app pnpm db:push
```

سيصبح الموقع متاحًا على المنفذ `3000`. يمكنك وضع خادم وكيل (Reverse Proxy) مثل Nginx أو Caddy أمامه لإدارة شهادات SSL (HTTPS).

---

## 3. التحقق من صحة النشر

يتضمن المشروع سكربت للتحقق من جاهزية الإنتاج:
```bash
docker-compose exec app pnpm preflight:production
```
سيقوم هذا السكربت بفحص المتغيرات والتأكد من عدم نسيان أي إعداد حرج (مثل ضعف الـ JWT أو غياب مفاتيح البريد).
